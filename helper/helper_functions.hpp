#ifndef HELPER_FUNCTIONS_HPP
#define HELPER_FUNCTIONS_HPP

#include <vector>
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

// Function declaration
std::vector<ll> calculating_stride(std::vector<ll> shape);
void set_all(std::vector<double>& a, double toset);
void grad_flow_for_add(std::vector<double> &a,std::vector<double> &b);
#endif 